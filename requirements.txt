fastapi
uvicorn
python-multipart
azure-storage-blob[aio]
redis

# Dependencies from pipeline.py (You MUST add all of them)
numpy
SimpleITK
scikit-image
vtk
pydicom
nibabel
# pyvista # CRITICAL: This library is not compatible with Vercel's headless serverless environment and will cause runtime errors.
# You must refactor your pipeline to use a headless-compatible library for mesh generation.